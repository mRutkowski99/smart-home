# shared-requests

This library was generated with [Nx](https://nx.dev).

## Building

Run `nx build shared-requests` to build the library.

## Running unit tests

Run `nx test shared-requests` to execute the unit tests via [Jest](https://jestjs.io).
