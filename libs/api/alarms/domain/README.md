# api-alarms-domain

This library was generated with [Nx](https://nx.dev).

## Building

Run `nx build api-alarms-domain` to build the library.

## Running unit tests

Run `nx test api-alarms-domain` to execute the unit tests via [Jest](https://jestjs.io).
